# XYZW 项目开发学习计划：从编程入门到独立贡献

> 适用对象：编程初学者，希望看懂并参与本项目开发。  
> 制定日期：2026-09-15；依据当前仓库提交 `2e1b7b8`、实际源码与开发配置整理。  
> 建议节奏：每周 8～10 小时，主线约 20 周；每周 4～5 小时可延长至约 40 周。这是学习预算，不是掌握速度的保证。  
> 核心目标：能解释一个功能的数据流，独立完成小需求、定位常见问题、验证修改并提交代码。

## 目录

1. [如何使用这份计划](#如何使用这份计划)
2. [先认识这个项目](#先认识这个项目)
3. [20 周学习总表](#20-周学习总表)
4. [分阶段学习提纲与练习](#分阶段学习提纲与练习)
5. [项目源码阅读路线](#项目源码阅读路线)
6. [逐文件学习流程与代表性代码](#逐文件学习流程与代表性代码)
7. [第一次启动项目](#第一次启动项目)
8. [从练习到真实贡献](#从练习到真实贡献)
9. [调试与验证方法](#调试与验证方法)
10. [延伸学习资料库](#延伸学习资料库)
11. [每周执行与毕业检查](#每周执行与毕业检查)

## 如何使用这份计划

**你不需要读懂全部源码，才开始参与开发。** 先完成一个小功能的完整闭环，再逐渐扩大负责范围。

- 每周按“读资料 → 写小练习 → 对照项目 → 自测”的顺序学习。
- 先做每阶段的必学内容；资料库用于遇到问题时查阅，不需要全部通读。
- 每阶段都有验收标准。达不到时增加练习时间，不必按日期强行推进。
- 基础练习放在仓库外的独立练习目录；真正要提交的改动放在项目分支中。
- 练习使用虚构角色和模拟响应。界面练习不需要真实游戏 Token；真实账号操作留到理解调用链之后。
- 遇到英文文档可以用浏览器翻译，保留 `ref`、`Promise` 等代码名方便搜索。

### 三个能力里程碑

| 里程碑 | 参考时间 | 可以开始承担的工作 |
| --- | --- | --- |
| 会运行、会阅读小段代码 | 第 1～6 周 | 文档、简单文案和样式修改 |
| 会独立开发界面功能 | 第 7～14 周 | 展示组件、搜索筛选、表单提示、纯函数修复 |
| 能追踪完整业务链路 | 第 15～20 周 | 带状态管理和异常处理的小功能、任务相关修复 |

“完整学习”是持续深化的过程。20 周主线旨在形成独立贡献能力；协议、复杂并发和后端可以在此后继续深入。

## 先认识这个项目

这是一个以 Vue 3 为主要界面框架的游戏辅助应用。用户导入 Token、管理角色，并通过 WebSocket 与游戏服务通信。

### 用普通语言理解技术分工

| 名称 | 可以怎样理解 | 在本项目中的作用 |
| --- | --- | --- |
| HTML | 页面上的内容和结构 | 标题、按钮、输入框、列表 |
| CSS / SCSS | 内容的外观与排列规则 | 卡片、布局、手机适配、主题颜色 |
| JavaScript | 页面执行的动作 | 点击处理、筛选列表、等待请求结果 |
| TypeScript | 给 JavaScript 增加类型约束 | 描述 Token 和连接对象，提前发现部分错误 |
| Vue 3 | 将数据与界面连接起来的框架 | 数据变化后更新组件显示 |
| 组件 | 可复用的一块界面 | 主题按钮、角色卡片、任务卡片 |
| Pinia Store | 多个页面共同使用的数据与操作 | 保存 Token、选中角色、连接状态 |
| Vue Router | 网址与页面之间的映射 | 从首页进入 Token 管理或任务页面 |
| VueUse | 常用响应式工具集合 | 将部分状态与浏览器存储连接 |
| Vite | 开发和打包工具 | 启动本地网页，将源码构建为部署文件 |
| WebSocket | 持续保持的双向通信连接 | 发送游戏命令、接收服务端消息 |
| BON | 项目使用的二进制消息格式 | 将对象转换成通信数据，并解析收到的数据 |
| Git / PR | 修改历史 / 代码合入申请 | 保存版本，让其他开发者审查改动 |

### 一条值得反复追踪的主线

```text
用户在页面中操作
    ↓
Vue 组件的事件处理函数
    ↓
tokenStore 中的操作与状态
    ↓
WebSocket 客户端、命令队列、BON 编解码
    ↓
游戏服务响应
    ↓
状态更新 → Vue 界面更新

部分状态还会保存到 localStorage 或 IndexedDB，供下次打开时恢复。
```

第一遍理解每层负责什么即可，后续再研究队列、超时和协议细节。

### 当前代码中需要知道的差异

- 根目录声明 Vue 3.5、Vue Router 4、Pinia 2、Vite 5、TypeScript 5，以及 Naive UI 和 Arco Design 两套组件库。版本范围见 [package.json](./package.json)，锁定依赖见 [pnpm-lock.yaml](./pnpm-lock.yaml)。学习时核对版本，不要为了跟教程一致而直接升级项目依赖。
- `packageManager` 指定 `pnpm@10.19.0`。本次本机检查结果是 Node.js `v24.13.0`、pnpm `10.19.0`；这只是当前环境记录，不代表已验证所有构建和测试。
- 实际主题按钮位于 [src/components/Common/ThemeToggle.vue](./src/components/Common/ThemeToggle.vue)。有些旧说明中的路径已经不同。
- 仓库有基于 `node:test` 的自动化测试，入口是 `pnpm test`。
- 项目还有 [render-backend/package.json](./render-backend/package.json) 声明的 Express 后端，以及 Gist 备份、HTTP 接口代码。理解整个仓库时，不能把所有功能都视为纯本地操作。前端基础阶段先学习界面与模拟数据即可。
- [src/main.js](./src/main.js) 中的 i18n 注册目前被注释。新增文案前先看所在模块做法，不要假设 `$t()` 已经能直接运行。

## 20 周学习总表

| 周数 | 学习重点 | 本周应产出的成果 |
| --- | --- | --- |
| 1 | 文件、终端、编辑器、浏览器、Git 入门 | 启动项目；记录目录作用和启动步骤 |
| 2 | HTML、表单、CSS 选择器、盒模型 | 静态角色卡片 |
| 3 | Flex 布局、响应式、基础可访问性 | 手机宽度可用的角色列表页 |
| 4 | JavaScript 变量、条件、循环、函数 | 角色数据统计与格式化函数 |
| 5 | 数组、对象、模块、DOM、事件 | 原生 JavaScript 角色搜索练习 |
| 6 | Promise、async/await、异常处理 | 模拟加载角色：成功、失败、重试 |
| 7 | Vue 单文件组件、响应式、模板 | 把角色卡片改写成 Vue 组件 |
| 8 | props、事件、表单、计算属性 | 可搜索的 Vue 角色列表 |
| 9 | 生命周期、组合式函数、主题、UI 组件 | 能解释并调整主题按钮的小功能 |
| 10 | Vue Router、路由参数、守卫 | 独立练习中的列表页与详情页 |
| 11 | Pinia、共享状态、VueUse 存储 | 刷新后保留选择的模拟角色管理器 |
| 12 | TypeScript 基础、联合类型、空值 | 为模拟角色和连接状态增加类型 |
| 13 | 开发工具、断点、日志、错误定位 | 一份可以复现问题的缺陷报告 |
| 14 | 单元测试、分支、提交、PR | 完成一个小修复及相关验证 |
| 15 | Token 导入、JSON、Base64、存储 | 导入流程图与异常输入案例表 |
| 16 | WebSocket、事件、请求响应、超时 | 可模拟连接状态的展示组件 |
| 17 | 排队、并发限制、重连、连接清理 | 用模拟请求解释队列行为 |
| 18 | 日常任务、进度、失败与取消 | 三步模拟任务运行器 |
| 19 | BON、字节数组、编码与解码 | 一个本地编解码往返实验 |
| 20 | 独立完成小需求 | 可审查的改动、验证记录与说明 |

## 分阶段学习提纲与练习

### 阶段一：环境与网页基础｜第 1～3 周

**必学提纲**

1. 文件与目录：绝对路径、相对路径、扩展名、当前工作目录。
2. 工具分工：编辑器写代码；终端运行工具；浏览器显示结果。
3. HTML：标题、段落、链接、按钮、表单、`label`、列表。
4. CSS：选择器、优先级、盒模型、间距、Flex、媒体查询。
5. Git：工作区、暂存区、提交；学会查看 `status` 和 `diff`。

**练习**

- 在独立目录做一个角色卡片，展示姓名、服务器、等级和状态。
- 将三张卡片排成一行，窄屏时改为一列。
- 为输入框提供标签，用键盘 Tab 能移动到按钮；不要只靠颜色表达状态。
- 启动本项目，认识首页与 `/tokens` 页面，暂时无需导入账号。

**对照源码**：[src/views/Home.vue](./src/views/Home.vue)、[src/assets/styles/variables.scss](./src/assets/styles/variables.scss)。这一阶段只读模板和样式。

**验收**

- [ ] 能解释 `margin` 和 `padding` 的区别。
- [ ] 能独立做一张卡片并解释其布局。
- [ ] 知道在哪里查看浏览器报错、如何停止开发服务。
- [ ] 能通过 Git 查看自己具体改了哪些内容。

**资料**：[MDN Web 开发入门](https://developer.mozilla.org/zh-CN/docs/Learn_web_development/Getting_started)、[MDN 第一个网站](https://developer.mozilla.org/zh-CN/docs/Learn_web_development/Getting_started/Your_first_website)、[Pro Git 中文版](https://git-scm.com/book/zh/v2)。先读入门部分。

### 阶段二：JavaScript 与异步编程｜第 4～6 周

**必学提纲**

- `const` / `let`、字符串、数字、布尔值、`null`、`undefined`。
- 条件、循环、函数、参数、返回值、作用域。
- 对象、数组、解构、展开语法；`map`、`filter`、`find`、`some`。
- `Set` 去重；对象引用与浅拷贝；避免意外修改传入数据。
- `import` / `export`、DOM 事件、JSON 序列化与解析。
- `Promise`、`async` / `await`、`try` / `catch` / `finally`。
- 理解“等待某次异步操作完成”与“阻塞整个页面”不是一回事。

**练习**

用三条虚构角色数据完成以下操作：

1. 按名字搜索，按服务器筛选，统计选中数量。
2. 输入为空时显示全部角色；无结果时显示提示。
3. 用 `Promise` 和定时器模拟加载延迟；主动制造失败，提供重试按钮。
4. 加载中禁用重复提交，结束后恢复按钮。

**对照源码**：[src/utils/gameSelection.js](./src/utils/gameSelection.js)。文件较短，适合逐行读；理解它为什么创建新的 `Set`。

**验收**

- [ ] 不看示例能写出筛选函数，并处理空数组。
- [ ] 能预测 `async` 函数返回的类型，以及失败时进入哪个分支。
- [ ] 能解释为什么 `forEach(async () => ...)` 不会让外层等待全部任务。
- [ ] 能区分“数据是对象”和“数据是 JSON 字符串”。

**资料**：[MDN JavaScript 指南](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide)。按语法、函数、对象、集合、Promise、模块的顺序选读；元编程和生成器可暂缓。

### 阶段三：Vue 组件开发｜第 7～9 周

**必学提纲**

- `.vue` 文件由 `template`、`script setup`、`style` 组成。
- `ref`、`reactive`、`computed`、`watch` 的用途；脚本中 `.value` 的含义。
- `v-if`、`v-for` 与稳定的 `key`、`v-model`、`:属性`、`@事件`。
- 父组件通过 props 传数据，子组件通过事件通知父组件。
- 生命周期、卸载时清理定时器与监听器。
- 组合式函数：把可复用的响应式逻辑提取到 `useXxx`。
- 使用项目现有组件库：查属性、事件、插槽，先学按钮、输入框、提示组件。

**练习**

把阶段二的角色管理练习改为 Vue：拆出 `RoleCard`，父组件负责列表与筛选，子组件负责展示和发出选中事件。增加加载、空结果和失败三种界面。

**对照源码**：[src/components/Common/ThemeToggle.vue](./src/components/Common/ThemeToggle.vue) → [src/composables/useTheme.js](./src/composables/useTheme.js) → [src/components/Daily/DailyTaskCard.vue](./src/components/Daily/DailyTaskCard.vue)。先读使用方，再读实现方。

**验收**

- [ ] 能解释为什么数据改变后界面会变化。
- [ ] 搜索结果通过 `computed` 得到，没有额外保存一份容易不同步的列表。
- [ ] 子组件不会直接修改父组件传入的业务数据。
- [ ] 修改后的界面在浅色、深色和窄屏下可用。

**资料**：[Vue 交互式教程](https://cn.vuejs.org/tutorial/)、[Vue 指南](https://cn.vuejs.org/guide/introduction)。教程选用组合式 API 和单文件组件模式，与项目的主要写法对应。

### 阶段四：路由、状态、存储与类型｜第 10～12 周

**必学提纲**

- Router：路径、页面组件、参数、查询字符串、导航守卫、懒加载。
- Pinia：定义 Store、状态、计算结果、操作方法；解构响应式状态时认识 `storeToRefs`。
- localStorage：键值存储、JSON、默认值、刷新恢复；存储内容可能缺失或损坏。
- IndexedDB：浏览器中的异步数据库，先理解用途，稍后再深入 API。
- TypeScript：基础类型、`interface`、可选属性、联合类型、类型缩小、基础泛型。
- 类型检查不能代替运行时校验：网络返回值仍可能与声明不一致。

**练习**

在独立练习中做“模拟角色管理器”：列表页和详情页共享一个 Store；选中角色后刷新可恢复；删除当前角色时同步清除选择；为角色和连接状态定义类型。

**对照源码**：[src/router/index.js](./src/router/index.js)、[src/stores/tokenStore.ts](./src/stores/tokenStore.ts)、[src/utils/tokenDb.js](./src/utils/tokenDb.js)。Store 第一遍只读类型、状态声明和 `addToken`、`updateToken`、`removeToken`、`selectToken`，其余按调用再跟进。

**验收**

- [ ] 能说清组件局部状态与 Store 共享状态的边界。
- [ ] 删除角色后不会保留无效选择；损坏存储能回到可用状态。
- [ ] 能识别一个可空字段，先判断再访问属性。
- [ ] 在真实项目中通过 Store 方法操作 Token，不直接改 `gameTokens`。

**资料**：[Vue Router 入门](https://router.vuejs.org/zh/guide/)、[Pinia 简介](https://pinia.vuejs.org/zh/introduction.html)、[VueUse useLocalStorage](https://vueuse.org/core/uselocalstorage/)、[TypeScript 手册](https://www.typescriptlang.org/docs/handbook/intro.html)。Pinia 在线主站目前是 v3，项目是 v2；先学共通概念，具体 API 和安装要求核对项目版本。

### 阶段五：调试、测试与协作｜第 13～14 周

**必学提纲**

- Console 看错误，Sources 设断点，Network 看请求，Application 看存储。
- 学会阅读错误堆栈，找到自己代码中的第一处相关调用。
- 单元测试：输入、预期输出、断言；关注边界和失败路径。
- Git：创建分支、查看差异、选择文件提交；理解 PR 是等待审查的合入请求。

**练习**

1. 在独立练习中制造一个“删除角色后选中数量不更新”的错误。
2. 写清复现步骤、预期和实际表现，用断点定位。
3. 阅读 [test/multiGameSelection.test.js](./test/multiGameSelection.test.js)，理解它怎样验证不修改原集合、保持顺序及移除无效选择。
4. 为一个逻辑修复编写能在修复前失败、修复后通过的测试。
5. 整理一个小改动，写出别人能读懂的修改说明。

**验收**

- [ ] 能稳定复现问题，而不只是描述“有时不好用”。
- [ ] 能解释测试保护了什么行为。
- [ ] 能检查提交差异，排除临时文件和无关格式改动。
- [ ] 能区分编译通过、逻辑测试通过、页面实际可用三种验证。

**资料**：[Node.js 测试运行器](https://nodejs.org/api/test.html)、[GitHub 创建 PR](https://docs.github.com/zh/pull-requests/how-tos/create-pull-requests/creating-a-pull-request)。本仓库已有 `node:test`，先沿用，无需为入门练习更换测试框架。

### 阶段六：Token 与实时通信｜第 15～17 周

**必学提纲**

- Token 导入：读取输入、识别格式、解析、校验、保存、选择。
- Base64 是编码，不是加密；解码成功也不代表 Token 有效。
- HTTP 请求与 WebSocket 长连接；打开、消息、错误、关闭事件。
- 请求与响应匹配、超时、心跳、重连、连接状态。
- 排队与并发：一次允许多少连接、何时发起下一次连接、任务结束如何释放资源。
- WebSocket 不沿用 fetch 的 CORS 检查流程，但服务端仍可检查 Origin 并进行鉴权，不能理解为没有访问限制。

**练习**

1. 用虚构 JSON/Base64 字符串做解析练习，覆盖空输入、损坏输入、缺字段。
2. 画出“导入”按钮到 Store 的调用过程，区分界面校验与存储方法各自做的事情。
3. 做一个只接收模拟状态的连接卡片：未连接、连接中、已连接、失败。
4. 用五个模拟异步任务和并发上限二做队列演示，记录开始/结束顺序。
5. 阅读项目真实命令注册，核对命令名和参数，不把文档示意命令直接用于账号操作。

**对照源码**：[src/views/TokenImport/manual.vue](./src/views/TokenImport/manual.vue) → [src/stores/tokenStore.ts](./src/stores/tokenStore.ts) → [src/utils/xyzwWebSocket.js](./src/utils/xyzwWebSocket.js) → [src/utils/wsAgent.js](./src/utils/wsAgent.js)。批量连接还可看 [src/utils/batch/connectionManager.js](./src/utils/batch/connectionManager.js)。

**验收**

- [ ] 能画出成功与失败两条数据流。
- [ ] 能解释“发送出去”与“收到成功响应”的区别。
- [ ] 能解释重试为什么可能导致重复执行，尤其是有副作用的游戏操作。
- [ ] 修改连接相关代码时沿用项目的连接协调机制，不另开连接绕过限制。

**资料**：[MDN WebSocket API](https://developer.mozilla.org/zh-CN/docs/Web/API/WebSockets_API)、[MDN Base64](https://developer.mozilla.org/en-US/docs/Glossary/Base64)、[p-queue 官方仓库](https://github.com/sindresorhus/p-queue)。项目使用 p-queue 7，阅读示例时注意版本。

### 阶段七：任务与二进制协议｜第 18～19 周

**必学提纲**

- 任务状态：未开始、执行中、成功、失败、取消。
- 顺序执行、命令间隔、超时、日志、进度回调、结束清理。
- 取消的边界：阻止后续步骤不等于撤回已经发给服务端的操作。
- 字节、`ArrayBuffer`、`Uint8Array`、`DataView`、文本编码。
- 编码、压缩、加密分别解决什么问题；先理解接口输入输出。

**练习**

- 做一个三步模拟任务运行器：读取资料、检查状态、更新模拟结果。显示进度、失败原因，允许取消后续步骤。
- 分别制造第二步失败、超时、用户取消，观察后续步骤是否执行、界面是否恢复。
- 用不包含真实账号的简单对象做 BON 编码再解码，比较结果；遇到不支持的类型时记录限制。

**对照源码**：[src/utils/dailyTaskRunner.js](./src/utils/dailyTaskRunner.js)、[src/utils/helperTaskRunner.js](./src/utils/helperTaskRunner.js)、[test/helperTaskRunner.test.js](./test/helperTaskRunner.test.js)、[src/utils/bonProtocol.js](./src/utils/bonProtocol.js)。

**验收**

- [ ] 失败或取消后，界面不会一直停留在“执行中”。
- [ ] 能区分对象、JSON 字符串与二进制数据。
- [ ] 能说明编解码实验验证了什么、没有验证什么。
- [ ] 读得懂协议入口与输出，不要求此时重写加密或压缩算法。

**资料**：[MDN JavaScript 类型化数组](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide/Typed_arrays)、[MDN TextEncoder](https://developer.mozilla.org/zh-CN/docs/Web/API/TextEncoder)。BON 的具体实现以仓库代码和已有测试为准。

### 阶段八：独立小需求｜第 20 周

从下节的候选任务中选一个，先确认当前版本是否已经具备该功能，再明确缺口。将范围控制在一周内。

**交付物**

1. 需求：谁在什么情况下遇到什么问题。
2. 方案：改哪些文件、数据如何流动、失败时如何表现。
3. 代码：范围清晰，复用现有组件和 Store 方法。
4. 验证：正常情况、空数据、失败情况；界面改动附主题和窄屏检查。
5. 说明：改了什么、为什么、如何验证、仍有哪些限制。

**验收**：别人只看你的改动和说明，就能复现原问题、理解解决方式，并验证结果。核心连接和任务逻辑建议请熟悉项目的人审查。

## 项目源码阅读路线

按“小组件 → 页面 → 状态 → 通信 → 协议”的顺序阅读。不要从最大的文件第一行读到最后一行。

| 顺序 | 文件 | 带着这个问题去读 |
| --- | --- | --- |
| 1 | [package.json](./package.json)、[vite.config.js](./vite.config.js) | 项目怎样启动？依赖和路径别名在哪里声明？ |
| 2 | [src/main.js](./src/main.js)、[src/App.vue](./src/App.vue) | 应用如何创建？注册了什么？页面容器在哪里？ |
| 3 | [ThemeToggle.vue](./src/components/Common/ThemeToggle.vue) | 点击怎样触发逻辑？图标为什么会变化？ |
| 4 | [useTheme.js](./src/composables/useTheme.js) | 主题状态从哪里来，如何同步到页面？ |
| 5 | [gameSelection.js](./src/utils/gameSelection.js) 及其 [测试](./test/multiGameSelection.test.js) | 简单业务函数怎样被验证？ |
| 6 | [路由配置](./src/router/index.js) | 一个网址对应哪个组件？哪些页面需要 Token？ |
| 7 | [手动导入页](./src/views/TokenImport/manual.vue)、[TokenManager.vue](./src/components/TokenManager.vue) | 用户输入如何变成业务操作？ |
| 8 | [tokenStore.ts](./src/stores/tokenStore.ts) | 哪些是状态、哪些是派生值、哪些是操作？ |
| 9 | [xyzwWebSocket.js](./src/utils/xyzwWebSocket.js) | 命令怎样发送，响应怎样找到等待它的人？ |
| 10 | [dailyTaskRunner.js](./src/utils/dailyTaskRunner.js) | 多个命令怎样组成任务，失败后怎样收尾？ |
| 11 | [bonProtocol.js](./src/utils/bonProtocol.js) | 对象与字节之间怎样转换？ |
| 12 | [备份管理](./src/utils/backup/backupManager.ts)、[后端入口](./render-backend/server.js) | 完成主线后，再研究备份和服务端职责 |

### 每读一个函数，记录五件事

```text
函数名：
谁调用它：
输入是什么：
返回什么，或者修改什么状态：
失败时如何处理：
```

例如读 `selectToken` 时，先看它被哪个按钮或页面调用，再看是否触发连接；不要只凭函数名字推断行为。

## 逐文件学习流程与代表性代码

本节把前面的路线拆成可以逐项完成的阅读任务。**下面是学习顺序，不是程序的执行顺序。** 每次完成一站，记录结果，再按所属周数进入下一站。

代码均为当前项目的真实摘录，未展示的上下文仍需回到源文件阅读。摘录用于讲解，不是可以单独粘贴运行的完整程序；额外练习会明确标为“练习代码”。代码行号以本次整理时为准，后续变化时用给出的函数名搜索。

### 文件学习顺序总清单

| 站点 / 对应周数 | 按箭头依次打开文件（路径相对于仓库根目录） | 第一遍只看哪里 | 完成后应产出 |
| --- | --- | --- | --- |
| 01 / 第 1 周 | [package.json](./package.json) → [index.html](./index.html) → [src/main.js](./src/main.js) → [src/App.vue](./src/App.vue) | scripts、`#app`、`createApp`、根模板 | 四个文件各用一句话说明作用 |
| 02 / 第 2～3 周 | [src/components/Common/ThemeToggle.vue](./src/components/Common/ThemeToggle.vue) → [src/assets/styles/variables.scss](./src/assets/styles/variables.scss) | template、style、主题变量 | 标注按钮结构与悬停样式 |
| 03 / 第 4～5 周 | [src/utils/gameSelection.js](./src/utils/gameSelection.js) → [test/multiGameSelection.test.js](./test/multiGameSelection.test.js) | 三个小函数、第一条测试 | 手算输入与输出；第 14 周再运行测试 |
| 04 / 第 7～9 周 | [src/components/Common/ThemeToggle.vue](./src/components/Common/ThemeToggle.vue) → [src/composables/useTheme.js](./src/composables/useTheme.js) | script setup、`toggleTheme`、`setDarkTheme`、生命周期 | 一张点击到主题变化的调用图 |
| 05 / 第 10 周 | [src/router/index.js](./src/router/index.js) → [src/views/TokenImport/index.vue](./src/views/TokenImport/index.vue) | `/tokens` 配置、对应页面模板 | 一个网址如何找到页面的解释 |
| 06 / 第 11～12 周 | [src/stores/tokenStore.ts](./src/stores/tokenStore.ts) → [src/views/TokenImport/manual.vue](./src/views/TokenImport/manual.vue) | 类型、派生状态、`updateToken`、表单绑定 | 字段与类型对照表 |
| 07 / 第 14～15 周 | [src/views/TokenImport/manual.vue](./src/views/TokenImport/manual.vue) → [src/stores/tokenStore.ts](./src/stores/tokenStore.ts) → [src/utils/token.ts](./src/utils/token.ts) | `handleImport`、`addToken`、`selectToken`、`transformToken` | 区分保存输入和连接前转换两条路径 |
| 08 / 第 16 周 | [src/stores/tokenStore.ts](./src/stores/tokenStore.ts) → [src/utils/xyzwWebSocket.js](./src/utils/xyzwWebSocket.js) | `sendMessageWithPromise`、`sendWithPromise`、`_handlePromiseResponse` | 请求序号与响应匹配示意 |
| 09 / 第 17 周 | [src/utils/xyzwWebSocket.js](./src/utils/xyzwWebSocket.js) → [src/utils/batch/connectionManager.js](./src/utils/batch/connectionManager.js) | 发送队列、连接相关入口及其调用者 | 排队、发送、响应的时间线 |
| 10 / 第 18 周 | [src/utils/dailyTaskRunner.js](./src/utils/dailyTaskRunner.js) → [test/helperTaskRunner.test.js](./test/helperTaskRunner.test.js) → [src/utils/helperTaskRunner.js](./src/utils/helperTaskRunner.js) | `executeGameCommand`、测试用例对应的方法 | 成功与失败的任务流程图 |
| 11 / 第 19 周 | [src/utils/xyzwWebSocket.js](./src/utils/xyzwWebSocket.js) → [src/utils/bonProtocol.js](./src/utils/bonProtocol.js) | `CommandRegistry`、`bon.encode` / `bon.decode` | 对象到字节再还原的实验记录 |
| 12 / 第 20 周以后 | [src/utils/backup/snapshotSchema.ts](./src/utils/backup/snapshotSchema.ts) → [src/utils/backup/backupManager.ts](./src/utils/backup/backupManager.ts) → [render-backend/package.json](./render-backend/package.json) → [render-backend/server.js](./render-backend/server.js) | 备份结构、管理入口、后端依赖与入口 | 前端、存储、外部服务的职责图 |

不用在第一周看懂所有文件。第 02 站先看按钮的 HTML/CSS；第 04 站回来再看同一个按钮的 Vue 和 JavaScript，这是有意安排的重复。

### 01｜应用入口：网页是怎样启动的

**源码**：[src/main.js](./src/main.js)；第 12 行起，搜索 `// 创建应用实例`。

```javascript
// 创建应用实例
const app = createApp(App);

// 使用插件
app.use(createPinia());
app.use(router);
app.use(naive);
```

**按这个顺序读：**

1. `createApp(App)`：以根组件 `App` 创建应用实例。
2. `app.use(createPinia())`：让应用能使用共享 Store。
3. `app.use(router)`：让应用能根据网址切换页面。
4. `app.use(naive)`：注册 Naive UI。继续向下找 `app.mount("#app")`，再到 `index.html` 找对应容器。

**小练习：** 画出 `index.html → main.js → App.vue → 页面组件`；检查 App 的模板实际如何容纳路由页面。先说出作用，不要求理解所有插件。

**过关问题：** `main.js` 和 `App.vue` 的职责有什么不同？

### 02｜单文件组件：结构、逻辑与样式放在一起

**源码**：[src/components/Common/ThemeToggle.vue](./src/components/Common/ThemeToggle.vue)；第 1 行起，搜索 `<template>`。

```vue
<template>
  <n-button circle size="medium" class="theme-toggle" @click="toggleTheme">
    <template #icon>
      <n-icon v-if="isDark">
        <Sunny />
      </n-icon>
      <n-icon v-else>
        <Moon />
      </n-icon>
    </template>
  </n-button>
</template>

<script setup>
import { Moon, Sunny } from "@vicons/ionicons5";
import { useTheme } from "@/composables/useTheme";

const { isDark, toggleTheme } = useTheme();
</script>

<style scoped lang="scss">
.theme-toggle {
  transition: all 0.3s ease;

  &:hover {
    transform: scale(1.1);
  }
}
</style>
```

**按这个顺序读：**

1. `<template>` 描述按钮；`@click` 把点击关联到函数。
2. `v-if` / `v-else` 根据 `isDark` 选择图标；插槽 `#icon` 把内容放进按钮的图标区域。
3. `<script setup>` 取得状态与切换函数；此时只需知道 `useTheme()` 提供了它们。
4. `style scoped` 限定样式作用范围；SCSS 的 `&:hover` 表示当前元素被鼠标悬停。

**小练习：** 在练习分支中将悬停缩放改为 `1.05`，观察前后差异，并用 `git diff` 确认只改变预期位置。

**过关问题：** 当前深色状态为什么显示太阳？结合点击后的目标状态解释图标含义。

### 03｜纯函数：从输入推导输出

**源码**：[src/utils/gameSelection.js](./src/utils/gameSelection.js)；第 1 行起，搜索 `export function toggleTokenSelection`。

```javascript
export function toggleTokenSelection(current, tokenId, checked) {
  const next = new Set(current);
  if (checked) next.add(tokenId);
  else next.delete(tokenId);
  return next;
}
```

**逐句理解：**

- `current` 是已选 ID 的集合，`tokenId` 是本次操作的 ID，`checked` 表示选中或取消。
- `new Set(current)` 创建新集合；后面的添加和删除发生在新集合上。
- `return next` 返回结果。这个函数不操作页面、不写存储、不连接服务器。

**手算练习：** 输入集合是 `{"a"}` 时，分别计算选中 `"b"`、取消 `"a"`、再次选中 `"a"` 的结果。三个实验都从原始集合开始。

**参考答案：** 分别为 `{"a", "b"}`、空集合、`{"a"}`；原始集合仍为 `{"a"}`。

**继续阅读：** 同文件的 `selectAllTokenIds` 与 `pruneTokenSelection`。解释 `map`、`filter` 和 `Set` 怎样配合。

**过关问题：** 如果直接操作 `current`，调用方会受到什么影响？

### 04｜测试：用断言表达应该保持的行为

**源码**：[test/multiGameSelection.test.js](./test/multiGameSelection.test.js)；第 10 行起，搜索 `test("token selection helpers do not mutate`。

```javascript
test("token selection helpers do not mutate the original set", () => {
  const current = new Set(["a"]);
  const selected = toggleTokenSelection(current, "b", true);
  const removed = toggleTokenSelection(selected, "a", false);

  assert.deepEqual([...current], ["a"]);
  assert.deepEqual([...selected], ["a", "b"]);
  assert.deepEqual([...removed], ["b"]);
});
```

**逐句理解：** 先准备原始集合，再执行两次操作，最后比较原集合与两个结果。`[...集合]` 把集合展开为数组，`assert.deepEqual` 检查结构与内容。

**小练习：** 先预测三条断言的结果，再阅读函数实现。学习到测试阶段后，可在项目根目录只运行这个文件：

```bash
node --experimental-loader ./test/alias-loader.mjs --test test/multiGameSelection.test.js
```

这里保留项目的别名加载器，因为测试从 `@/utils/gameSelection` 导入模块。此命令是供学习时执行的指引，本次文档更新没有运行它。

**过关问题：** 第一条断言为什么不能省略？它保护的是“输入保持不变”，不只是返回值。

### 05｜响应式逻辑：一次点击如何改变主题

**源码**：[src/composables/useTheme.js](./src/composables/useTheme.js)；第 74 行起，搜索 `// 切换主题`。

```javascript
  // 切换主题
  const toggleTheme = () => {
    if (isDark.value) {
      setLightTheme();
    } else {
      setDarkTheme();
    }
  };
```

**完整阅读顺序：**

1. 文件顶部 `const isDark = ref(false)`：它定义在函数外，因此多次调用 `useTheme()` 使用同一状态。
2. 上面的 `toggleTheme`：依据当前状态选择分支。
3. `setDarkTheme` / `setLightTheme`：修改 DOM 属性、保存偏好、更新 `isDark.value`。
4. `onMounted` / `onUnmounted`：观察初始化和资源清理。

**小练习：** 点击一次主题按钮，在浏览器中观察页面根元素的 `data-theme`、localStorage 的 `theme` 和图标变化。

**源码边界：** 函数“被定义”不代表它已执行。例如 `setupSystemThemeListener` 被返回，但是否调用要继续搜索使用方；不能只读函数名称就断定监听一定生效。

**过关问题：** `isDark.value` 更新和 `localStorage.setItem` 分别解决什么问题？

### 06｜路由：网址怎样对应到页面

**源码**：[src/router/index.js](./src/router/index.js)；第 19 行起，搜索 `path: '/tokens',`。

```javascript
    path: '/tokens',
    name: 'TokenImport',
    component: () => import('@/views/TokenImport/index.vue'),
    meta: {
      title: 'Token管理',
      requiresToken: false
    },
    props: route => ({
      token: route.query.token,
      name: route.query.name,
      server: route.query.server,
      wsUrl: route.query.wsUrl,
      api: route.query.api,
      auto: route.query.auto === 'true'
    })
  },
```

这是路由对象的内部片段。

- `path` 是匹配的网址路径；`component` 用动态导入加载页面。
- `meta.requiresToken` 是自定义信息，需要导航守卫读取才会产生限制。
- `props` 将网址查询参数传给页面；`auto` 通过与字符串 `"true"` 比较，转换成布尔值。

**小练习：** 不实际导入任何账号，纸上推演 `/tokens?name=练习角色&auto=false`。`name` 与 `auto` 会传成什么值？

**参考答案：** `name` 为字符串 `"练习角色"`，`auto` 为布尔值 `false`。注意字符串 `"false"` 本身不是布尔值。

**过关问题：** 删除查询参数和传入空字符串是否一定相同？去页面查看默认值如何处理。

### 07｜Store：保存状态与计算结果

**源码**：[src/stores/tokenStore.ts](./src/stores/tokenStore.ts)；第 72 行起，搜索 `export const gameTokens =`。

```typescript
export const gameTokens = useLocalStorage<TokenData[]>("gameTokens", []);
export const hasTokens = computed(() => gameTokens.value.length > 0);
export const selectedTokenId = useLocalStorage("selectedTokenId", "");
export const selectedToken = computed(() => {
  return gameTokens.value?.find((token) => token.id === selectedTokenId.value);
});
```

**按这个顺序读：**

1. `TokenData[]` 表示 Token 对象数组；回到文件顶部查看 `TokenData` 字段。
2. `useLocalStorage` 将数组和当前选择 ID 与浏览器存储关联。
3. `hasTokens` 是根据数组长度计算的结果，不需要再手工维护一个布尔开关。
4. `selectedToken` 用 ID 查找当前对象；找不到时可能返回 `undefined`。

**小练习：** 纸上列出“列表为空”“有两个角色但未选中”“选中 ID 不在列表里”时两个计算结果的值。

**注意：** 这里展示的是 Store 实现中的状态声明。业务组件仍通过 `useTokenStore()` 和其方法使用数据，不在组件里直接修改 `gameTokens`。

**过关问题：** 为什么只保存选中 ID，再通过列表查找对象？这能减少哪种重复数据？

### 08｜更新数据：对象展开与可选字段

**源码**：[src/stores/tokenStore.ts](./src/stores/tokenStore.ts)；第 235 行起，搜索 `const updateToken =`。

```typescript
  const updateToken = (tokenId: string, updates: Partial<TokenData>) => {
    const index = gameTokens.value.findIndex((token) => token.id === tokenId);
    if (index !== -1) {
      gameTokens.value[index] = {
        ...gameTokens.value[index],
        ...updates,
        updatedAt: new Date().toISOString(),
      };
```

**逐句理解：**

- `Partial<TokenData>` 表示更新对象可以只包含 Token 的部分字段。
- `findIndex` 找不到时返回 `-1`。
- 先展开原对象，再展开更新对象，因此更新字段覆盖同名旧字段。
- `updatedAt` 放在最后，以本次时间覆盖同名输入。
- 返回布尔值表示是否找到并更新；这里没有返回更新后的对象。

**小练习：** 纸上模拟原对象含 `name: "旧名称"`、`server: "一区"`，更新只有 `name: "新名称"`。列出更新后的字段。

**过关问题：** 如果把两个展开语句调换顺序，为什么新名称可能被旧名称覆盖？

### 09｜表单提交：沿着事件找到真正的业务入口

**源码**：[src/views/TokenImport/manual.vue](./src/views/TokenImport/manual.vue)；第 135 行起，搜索 `const handleImport =`。

```typescript
const handleImport = () => {
  isImporting.value = true;
  try {
    tokenStore.addToken({
      name: importForm.name,
      token: importForm.base64Token,
      server: importForm.server,
      wsUrl: importForm.wsUrl,
    });
    message.success("Token添加成功");
    importForm.name = "";
    importForm.base64Token = "";
    importForm.server = "";
    importForm.wsUrl = "";
    $emit("ok");
  } catch (error: any) {
    message.error(`添加Token失败: ${error.message || error}`);
  } finally {
    isImporting.value = false;
  }
};
```

**完整阅读顺序：** 输入框的 `v-model:value` → `importForm` → 按钮的 `@click="handleImport"` → `tokenStore.addToken` → 父组件处理 `ok` 事件的位置。

- `try` 是正常路径，`catch` 显示错误，`finally` 恢复加载状态。
- 成功后清空表单，并通过 `$emit("ok")` 通知父组件；父组件决定下一步。
- 当前方法是同步函数，调用 `addToken` 没有 `await`。不要据此理解成已经完成远端鉴权或成功连接游戏。

**源码边界：** 此处虽定义了 `importRules`，但 `handleImport` 中没有显式调用 `importFormRef.validate()`。需要区分字段触发的校验和提交前阻止非法输入；这段摘录不能证明提交前已完成完整校验。

**小练习：** 在独立模拟表单中让添加方法主动抛错，检查错误提示与加载状态。不要用真实 Token 做故障练习。

**过关问题：** 如果未来添加方法改成异步，而这里仍没有 `await`，当前 `catch` 与成功提示会有什么问题？

### 10｜异步请求：序号、等待与超时

**源码**：[src/utils/xyzwWebSocket.js](./src/utils/xyzwWebSocket.js)；第 869 行起，搜索 `sendWithPromise(cmd,`。

```javascript
  sendWithPromise(cmd, params = {}, timeoutMs = 5000) {
    return new Promise((resolve, reject) => {
      if (!this.connected && !this.socket) {
        return reject(new Error("WebSocket 连接已关闭"));
      }

      // 为此请求生成唯一的seq值
      const requestSeq = ++this.seq;

      // 设置 Promise 状态，使用seq作为键
      this.promises[requestSeq] = { resolve, reject, originalCmd: cmd };

      // 超时处理
      const timer = setTimeout(() => {
        delete this.promises[requestSeq];
        reject(new Error(`请求超时: ${cmd} (${timeoutMs}ms)`));
      }, timeoutMs);

      // 发送消息，直接传递seq
      this.send(cmd, params, {
        seq: requestSeq,
        onSent: () => {
          // 消息发送成功后，不要清除超时器，让它继续等待响应
          // 只有在收到响应或超时时才清除
        },
      });
    });
  }
```

**按这个顺序读：**

1. 创建 Promise，保存成功与失败时要调用的函数。
2. 为请求生成 `requestSeq`，把等待中的请求登记到 `this.promises`。
3. 启动计时器；超时删除登记，并以错误结束等待。
4. 调用 `send` 将命令加入发送流程；这一步不代表服务端已返回。
5. 跳到 `_handlePromiseResponse`，看实际实现如何用 `packet.resp` 查找请求并调用 `resolve` 或 `reject`。

**小练习：** 画出“登记请求 101 → 排队发送 → 收到 resp=101 → 完成 Promise”；再画出超时路径。当前计时器在入队前启动，等待队列的时间也占用超时预算。

**源码边界：** 此片段创建了局部 `timer`，但没有把它存入请求登记。结合响应处理路径继续检查清理方式，不要把这段代码当作已经完善的计时器清理范例。

**过关问题：** 两次同名命令为什么不能只靠命令名区分响应？

### 11｜任务封装：等待结果、间隔与错误传播

**源码**：[src/utils/dailyTaskRunner.js](./src/utils/dailyTaskRunner.js)；第 140 行起，搜索 `async executeGameCommand(`。

```javascript
  async executeGameCommand(
    tokenId,
    cmd,
    params = {},
    description = "",
    timeout = 8000,
  ) {
    try {
      if (description) this.log(`执行: ${description}`);
      const result = await this.tokenStore.sendMessageWithPromise(
        tokenId,
        cmd,
        params,
        timeout,
      );
      await new Promise((resolve) => setTimeout(resolve, this.delaySettings.commandDelay));
      if (description) this.log(`${description} - 成功`, "success");
      return result;
    } catch (error) {
      if (description) {
        const token = this.tokenStore.gameTokens.find((t) => t.id === tokenId);
        const tokenName = token?.name || tokenId;
        this.log(`[${tokenName}] ${description} - 失败: ${error.message}`, "error");
      }
      throw error;
    }
  }
```

这是类中的一个方法，需要结合类实例阅读。

- `await sendMessageWithPromise(...)` 等待本次命令的响应。
- 第二个 `await` 在成功后增加命令间隔，减少连续操作频率。
- `return result` 将结果交给上层任务。
- `catch` 记录日志后再次 `throw error`，让调用方有机会停止或处理失败。

**小练习：** 在独立练习中用模拟 Store 返回固定数据，再改成拒绝的 Promise，比较日志顺序。此实现的失败分支不会执行成功后的延迟。

**过关问题：** 如果删掉 `throw error`，上层是否还知道命令失败？为什么这可能让后续任务错误地继续执行？

### 12｜协议入口：先理解输入输出，再看算法

**源码**：[src/utils/bonProtocol.js](./src/utils/bonProtocol.js)；第 512 行起，搜索 `export const bon =`。

```javascript
export const bon = {
  encode: (value, clone = true) => {
    _enc.reset();
    _enc.encode(value);
    return _enc.getBytes(clone);
  },
  decode: (bytes) => {
    _dec.reset(bytes);
    return _dec.decode();
  },
};
```

**按这个顺序读：** `bon.encode` → `BonEncoder.encode` → `getBytes`；`bon.decode` → `BonDecoder.decode`。第一遍只记录输入输出，暂时跳过具体字节类型分支。

- 编码前 `reset()` 清理编码器状态，随后写入数据并取出字节。
- 解码时用字节重置解码器，再恢复值。
- 这只是 BON 序列化入口。完整网络包的封装、加密等步骤要继续阅读 `CommandRegistry` 和 `g_utils`，不能混为一个步骤。

**练习代码（新增示例，不是源码摘录）：** 在理解模块导入后，将下面代码放入练习文件，调整导入路径使其指向本仓库的 `bonProtocol.js`，使用虚构数据做实验。

```javascript
// bon 需从本仓库的 bonProtocol.js 导入。
const input = { name: "demo", level: 10 };
const bytes = bon.encode(input);
const output = bon.decode(bytes);
console.log(bytes instanceof Uint8Array);
console.log(output.name, output.level);
```

**预期：** 字节类型检查为 `true`，还原字段为 `demo` 与 `10`。本次只增加学习示例，未运行此实验；它不验证真实游戏服务是否接受该对象。

**过关问题：** 为什么编码后的字节不能直接当普通字符串处理？

### 把代表性代码串成三条学习闭环

**闭环 A：界面与状态（先完成）**

```text
ThemeToggle.vue 的 @click
  → useTheme.js 的 toggleTheme
  → setDarkTheme / setLightTheme
  → DOM 属性 + 存储 + isDark 更新
  → 按钮图标和页面外观变化
```

**闭环 B：输入与存储（再完成）**

```text
manual.vue 的 v-model:value
  → importForm
  → handleImport
  → tokenStore.addToken
  → gameTokens 的持久化状态
  → 依赖这些状态的列表更新
```

这条路径表示保存输入。连接前 Token 转换需要继续从 `selectToken` 追到连接逻辑及 `transformToken`；不要把所有环节都归到 `handleImport`。

**闭环 C：任务与响应（最后完成）**

```text
任务调用 executeGameCommand
  → tokenStore.sendMessageWithPromise 检查连接等条件
  → 客户端 sendWithPromise 登记请求
  → send / 发送队列 / 编码 / socket 发送
  → 收到并解析响应
  → _handlePromiseResponse 匹配请求
  → Promise 完成或失败
  → 任务返回结果或抛出错误
```

每完成一条闭环，关掉源码，用自己的话讲一遍：从哪里开始，经过什么状态，在哪里可能失败，结果怎样回到页面。讲不清的地方，就是下一次阅读的起点。

## 第一次启动项目

### 1. 确认工具

本机已经检测到 Node.js 与符合项目要求的 pnpm。换电脑时可参考 [Node.js 下载页](https://nodejs.org/en/download) 与 [pnpm 安装文档](https://pnpm.io/installation)，再核对项目版本约束。

在终端逐条执行：

```bash
cd /Users/liujianzhe/Documents/GitHub/xyzw_web_helper
node --version
pnpm --version
git status
```

路径是你当前电脑上的仓库位置；换电脑需要替换。`git status` 先帮助你确认已有改动，避免混入本次练习。

### 2. 安装并运行

```bash
pnpm install --frozen-lockfile
pnpm dev
```

- `--frozen-lockfile` 要求依赖声明与锁文件一致；如果失败，先阅读错误、确认分支和 pnpm 版本，不要直接删除锁文件。
- 默认开发端口为 3000，通常访问 [本地开发首页](http://localhost:3000)。端口被占用时以终端实际输出为准。
- 在运行服务的终端按 `Ctrl+C` 停止服务。
- 更换包管理器可能产生不同依赖解析；练习统一使用 pnpm。
- 直接在现有仓库运行即可，不需要执行脚手架命令重新创建或覆盖项目。

### 3. 建立修改前的验证基线

在另一个终端进入同一目录后，可执行：

```bash
pnpm test
pnpm build
```

记录修改前是否通过；如果已有失败，先保留错误信息，区分原有问题和本次修改造成的问题。本文只核对了命令配置，没有执行上述安装、构建或测试。

### 常用命令速查

| 命令 | 用途 | 需要注意 |
| --- | --- | --- |
| `pnpm dev` | 开发时查看页面 | 保存代码通常会触发页面更新 |
| `pnpm test` | 运行现有 `test/*.test.js` | 使用项目的别名加载器和 Node 测试运行器 |
| `pnpm build` | 生成生产构建 | 成功不等于所有业务逻辑正确 |
| `pnpm preview` | 预览已有构建结果 | 先构建；不等于发布到互联网 |
| `pnpm exec eslint src/components/Common/ThemeToggle.vue` | 检查一个文件 | 替换为你修改的文件；未添加自动修复参数 |
| `pnpm lint` | 检查并自动修复源码 | 当前脚本带 `--fix`，会改变文件，运行后检查差异 |
| `pnpm format` | 批量格式化源码 | 当前脚本调用 Prettier，但根目录未直接声明它；不作为首次启动必需步骤 |

`pnpm testr` / `pnpm testd` 是旧调试脚本：其中有占位输入，`testr` 还直接使用 Node 默认不认识的 `@utils` 别名。把它们作为后期研究材料，不作为新手环境是否安装成功的标准。

Vite 构建不等于完整的 Vue/TypeScript 类型检查。目前没有独立 `typecheck` 脚本；需要时再结合项目配置安排检查，不把单文件 `tsc` 命令当作完整项目验证。

## 从练习到真实贡献

### 候选任务：按难度递增

以下是练习方向，不表示当前版本一定存在相应缺陷。先检查已有实现，避免重复开发。

| 难度 | 候选任务 | 完成标准 |
| --- | --- | --- |
| 入门 | 补全运行说明、修正失效的项目文件路径 | 按文档能找到文件、理解步骤 |
| 入门 | 为图标按钮补充可访问名称 | 键盘可操作，辅助工具可理解用途 |
| 初级 | 优化某个卡片的窄屏布局 | 内容不溢出，深浅主题都清晰 |
| 初级 | 为已有列表补齐无结果提示 | 普通列表、空列表、搜索无结果三种情况明确 |
| 中级 | 改善角色搜索或筛选 | 组合条件正确，不改变原始 Token 数据 |
| 中级 | 优化导入错误提示 | 错误可理解，不在提示或日志中暴露完整 Token |
| 中级 | 展示更清晰的连接状态 | 复用已有状态，不自行创建额外连接 |
| 进阶 | 修复任务失败后进度不恢复的问题 | 先确认可复现；覆盖失败和正常路径 |

**建议第一个代码贡献：** 选一个小组件的可访问性或布局改进。熟悉提交过程后，再做状态和任务逻辑。

### 一次贡献的工作流程

1. 写下问题和验收条件，例如：“窄屏时卡片按钮被遮挡；期望全部可见且可点击”。
2. 查看工作区已有改动，创建学习分支：

   ```bash
   git status
   git switch -c codex/learn-first-contribution
   ```

3. 找到对应组件，追踪它依赖的数据和样式。
4. 做能解决问题的最小改动，保持项目当前写法。
5. 按改动类型验证：界面检查、相关逻辑测试、必要的构建检查。
6. 用 `git diff` 检查差异，只暂存这次需要提交的文件。
7. 提交后推送自己的分支；如无原仓库写权限，通过 Fork 发起 PR。
8. 根据审查意见继续修改，补充验证记录。

不要手工维护自动生成的 `src/auto-imports.d.ts`、`src/typed-router.d.ts` 等文件来实现业务功能。也不要在第一次贡献中进行全项目格式化或依赖大升级。

### PR 说明模板

```markdown
## 问题
在什么操作下出现什么现象？

## 修改
现在会如何表现？为什么这样处理？

## 验证
- 检查了哪些场景？结果是什么？
- 执行了哪些相关测试或构建？

## 限制
还有哪些情况尚未验证？没有则省略。
```

## 调试与验证方法

### 遇到问题，按这个顺序排查

1. **确认现象**：哪个页面、哪步操作、什么输入？记录预期与实际结果。
2. **缩小范围**：能否用一条模拟数据重现？是否只在刷新后或某种主题下发生？
3. **找触发点**：从模板的 `@click` 或输入绑定找到函数。
4. **看数据**：在断点处查看变量，不急于猜测修改。
5. **检查边界**：空值、失败、异步尚未完成、状态没有清理。
6. **验证修复**：原问题消失，正常路径仍然可用。

| 现象 | 优先检查 |
| --- | --- |
| 页面没有显示 | 终端构建错误、浏览器 Console、路由 |
| 点击没有反应 | 事件绑定、禁用状态、是否进入处理函数 |
| 数据改变但界面没变 | 是否仍有响应式关联、是否错误解构、模板实际绑定的变量 |
| 刷新后丢失数据 | 是否需要持久化、存储键、读取默认值、序列化 |
| 一直加载 | Promise 是否结束、失败处理、`finally` 是否清理状态 |
| WebSocket 操作失败 | 当前连接状态、命令注册、参数、超时与响应匹配 |

### 验证要与改动相匹配

- 文档或简单文案：检查内容、链接和差异，通常无需新写自动化测试。
- 样式或组件：浏览器检查正常、窄屏、深浅主题和键盘操作。
- 纯逻辑：测试空输入、正常输入、异常边界，并运行相关已有测试。
- 状态与异步：增加失败、重复操作、取消或资源清理场景。
- Token 与通信：用模拟数据验证解析和行为；真实连接验证需另行记录，不能由构建结果代替。

练习中不提交真实 Token、账号备份、完整请求地址中的凭据或含敏感信息的截图。Base64 包装不会让凭据变得安全。

## 延伸学习资料库

以官方资料为主，中文优先。每次只选择与当前练习直接相关的一两篇；以下链接是学习入口，不代表所有章节都需要读。核心主线入口已在整理时查询或打开核对；部分交互式网站无法由检索工具完整读取。

| 主题 | 链接 | 建议阅读范围 |
| --- | --- | --- |
| Web 入门 | [MDN 入门模块](https://developer.mozilla.org/zh-CN/docs/Learn_web_development/Getting_started) | 开发环境、文件、第一个网站 |
| HTML | [MDN HTML](https://developer.mozilla.org/zh-CN/docs/Web/HTML) | 常用元素、表单、语义 |
| CSS | [MDN CSS](https://developer.mozilla.org/zh-CN/docs/Web/CSS) | 盒模型、Flex、媒体查询 |
| JavaScript | [MDN 指南](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide) | 函数、对象、数组、Promise、模块 |
| Vue 3 | [交互教程](https://cn.vuejs.org/tutorial/) / [指南](https://cn.vuejs.org/guide/introduction) | 组合式 API、组件、响应式 |
| TypeScript | [官方手册，英文](https://www.typescriptlang.org/docs/handbook/intro.html) | Everyday Types、Narrowing、Object Types |
| 路由 | [Vue Router](https://router.vuejs.org/zh/guide/) | 基本导航、参数、导航守卫 |
| 状态管理 | [Pinia](https://pinia.vuejs.org/zh/introduction.html) | 定义 Store、State、Getter、Action；注意 v2/v3 差异 |
| 响应式存储 | [VueUse useLocalStorage，英文](https://vueuse.org/core/uselocalstorage/) | 基本用法与默认值 |
| 浏览器存储 | [MDN localStorage](https://developer.mozilla.org/zh-CN/docs/Web/API/Window/localStorage) / [IndexedDB](https://developer.mozilla.org/zh-CN/docs/Web/API/IndexedDB_API) | 先理解生命周期与异步操作 |
| HTTP 请求 | [MDN Fetch](https://developer.mozilla.org/zh-CN/docs/Web/API/Fetch_API/Using_Fetch) / [Axios](https://axios-http.com/zh/docs/intro) | 请求、响应和错误处理 |
| 实时通信 | [MDN WebSocket](https://developer.mozilla.org/zh-CN/docs/Web/API/WebSockets_API) | 连接事件与消息 |
| 二进制 | [类型化数组](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide/Typed_arrays) | ArrayBuffer、TypedArray、DataView |
| 打包工具 | [Vite 5 指南，英文](https://v5.vite.dev/guide/) | 项目使用的主版本、开发与构建 |
| 包管理 | [pnpm 安装](https://pnpm.io/installation) | 安装方式、版本约束 |
| Node.js | [官方下载](https://nodejs.org/en/download) / [测试运行器](https://nodejs.org/api/test.html) | 运行脚本与现有测试所用 API |
| UI 组件 | [Naive UI](https://www.naiveui.com/zh-CN/os-theme/docs/introduction) / [Arco Design Vue](https://arco.design/vue/docs/start) | 优先查当前组件已使用的库 |
| SCSS | [Sass 官方文档，英文](https://sass-lang.com/documentation/) | 变量、嵌套、模块 |
| 原子化样式 | [UnoCSS 文档](https://unocss.dev/guide/) | 认识已有类名，按项目版本核对配置 |
| 浏览器调试 | [Chrome DevTools](https://developer.chrome.com/docs/devtools?hl=zh-cn) | Console、Sources、Network |
| Vue 调试 | [Vue DevTools，英文](https://devtools.vuejs.org/guide/getting-started) | 组件树和状态观察 |
| 版本控制 | [Pro Git 中文版](https://git-scm.com/book/zh/v2) | 起步、Git 基础、Git 分支 |
| 协作提交 | [GitHub 创建 PR](https://docs.github.com/zh/pull-requests/how-tos/create-pull-requests/creating-a-pull-request) | 推送分支与提出合入申请 |

### 主线之后的进阶方向

- **界面方向**：无障碍、复杂表单、组件设计、性能分析、i18n 接入。
- **可靠性方向**：测试替身、状态机、跨标签页协调、连接恢复与幂等性。幂等性指重复执行不会不断产生额外效果。
- **协议方向**：字节序、压缩、协议边界校验、异常数据解析，结合本地 BON 实现学习。
- **后端方向**：先读后端依赖和入口，再学 [Express](https://expressjs.com/)、[Supabase](https://supabase.com/docs)、环境配置和部署。这是后续路线，不是参与前端小功能的前提。
- **备份方向**：阅读快照结构、导入校验、Gist 客户端，理解哪些数据离开浏览器以及如何恢复。

## 每周执行与毕业检查

### 每周 8～10 小时的分配

| 活动 | 建议时间 | 做法 |
| --- | --- | --- |
| 阅读与记录 | 2 小时 | 每次只解决一个概念，写自己的解释 |
| 编码练习 | 4～5 小时 | 从空文件写，遇到问题再查资料 |
| 对照项目 | 1～2 小时 | 读一个组件或一条调用链 |
| 自测复盘 | 1 小时 | 检查验收条件、记录问题和下一步 |

### 第一周每天做什么

1. 第一天：认识编辑器、终端和目录；确认 Node、pnpm、Git 可用。
2. 第二天：安装依赖并启动项目；写下实际访问地址和停止方法。
3. 第三天：看 `package.json`，用自己的话解释 `dev`、`test`、`build`。
4. 第四天：在独立练习目录写一个 HTML 页面，放标题、角色姓名和按钮。
5. 第五天：加入颜色、边框、内外边距。
6. 第六天：阅读主题按钮的模板，标出能看懂的部分与待学习的语法。
7. 第七天：复盘启动过程，列出三个具体问题，为第二周准备。

### 每周记录模板

```markdown
## 第 X 周
- 本周目标：
- 学会的三个概念（用自己的话）：
- 完成的练习或修改：
- 一个具体错误：现象 / 原因 / 修复方法
- 验收结果与未完成项：
- 下周最先解决的问题：
```

### 怎样使用 AI 辅助学习

- 读代码时：“请解释这个函数的输入、输出和副作用，再给一个虚构数据例子。”
- 做练习时：“先给我提示，不直接给完整答案。我实现后请检查边界情况。”
- 修错误时：“这是复现步骤和错误堆栈，请帮我缩小定位范围，并解释原因。”
- 提交前：“请审查这份差异是否有漏掉的失败路径和无关改动。”

对于准备提交的代码，你至少应能解释：为什么改这里、正常时怎么运行、失败时怎么办、如何验证。解释不出来的部分，先补一个小实验。

### 最终能力检查

- [ ] 我能独立启动项目，并解释主要开发命令。
- [ ] 我能从页面找到对应组件，读懂常见 Vue 写法。
- [ ] 我能独立写一个带输入、筛选和空状态的组件。
- [ ] 我能通过 Store 操作业务数据，处理持久化与无效选择。
- [ ] 我能读懂 Token 类型，并对不可信输入进行运行时检查。
- [ ] 我能追踪一次操作从按钮到状态再到界面更新的过程。
- [ ] 我能解释连接中、连接失败、超时和重连的区别。
- [ ] 我能编写有意义的逻辑测试，验证正常和失败路径。
- [ ] 我能提交一个范围清晰、说明完整的小改动。
- [ ] 我知道哪些部分尚未掌握，能借助源码、文档和审查继续推进。

完成这些检查后，你已经具备参与本项目日常开发的基础。后续每次选择一个更深一点的问题，逐步扩展到连接、任务、协议或后端。
